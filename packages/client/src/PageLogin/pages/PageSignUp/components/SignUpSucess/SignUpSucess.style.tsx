import makeStyles from "@mui/styles/makeStyles";
import { textAlign } from "@mui/system";

export const SignUpSucessStyle = makeStyles(theme => ({
  root: {

  },
  txts: {
    margin: "40px 0",
    textAlign: "center"

  }

}));